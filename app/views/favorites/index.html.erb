<p id="notice"><%= notice %></p>
<header>
<h1>お気に入り一覧</h1>
</header>

<table>
  <thead>
    <tr>
      <th>投稿者</th>
      <th>投稿内容</th>
      <th>画像</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @favorites.each do |favorite| %>
      <tr>
        <td><%= favorite.user.name %></td>
        <td><%= favorite.blog.content %></td>
        <td><%= image_tag favorite.blog.image.url %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '投稿一覧へ', blogs_path %>
<%= link_to '新規投稿へ', new_blog_path %>

<footer>
<p>fakebook</p>
</footer>