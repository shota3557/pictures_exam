<p id="notice"><%= notice %></p>

<header>
<h1>問い合わせ一覧</h1>
</header>

<table>
  <thead>
    <tr>
      <th>名前</th>
      <th>メールアドレス</th>
      <th>内容</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.name %></td>
        <td><%= contact.email %></td>
        <td><%= contact.content %></td>
        <td><%= link_to '問い合わせ内容', contact %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '問い合わせる', new_contact_path %>


<footer>
<p>fakebook</p>
</footer>